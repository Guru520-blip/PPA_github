/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable source maps in production to reduce bundle size on Vercel
  productionBrowserSourceMaps: false,
};

export default nextConfig;
